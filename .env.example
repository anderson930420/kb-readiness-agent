# Optional: only required when the corresponding real generative provider is used.
# The official deterministic baseline, extractive mode, and fake providers require
# no API key and do not load this file automatically.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MINIMAX_API_KEY=
MINIMAX_BASE_URL=https://api.minimax.io/v1
MINIMAX_MODEL=MiniMax-M3
MINIMAX_TIMEOUT_SECONDS=30
MINIMAX_MAX_RETRIES=2
MINIMAX_RETRY_BASE_SECONDS=0.5
